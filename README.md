# fleeced-repositron

A fleece for the ages.

## Usage

Install dependencies with Poetry and run the ingestion script:

```bash
poetry install
poetry run ingest-chatgpt
```

Run the Braki agent:

```bash
python -m agents.braki.main
```
